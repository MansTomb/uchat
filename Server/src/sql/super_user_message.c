#include "server.h"

cJSON *mx_super_user_message(sqlite3 *db, cJSON *jsn) {
    /*
     * Таня доделай, пожалуйста эту функцию. по сути ctrl+C ctrl+V send_message.c
     * но не нужно проверять роль юзера.на клиенте запрет выхода с этого канала
     */
}
