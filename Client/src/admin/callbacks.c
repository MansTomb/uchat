#include "client.h"

void mx_admine_send_on_click(GtkWidget *widget, gpointer data) {
    t_info *info = data;

    // Send meesage to all users
}
void mx_admin_logout_on_click(GtkWidget *widget, gpointer data) {
    t_info *info = data;

    // Back to login screen with logout
}
